apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: facility-manager-service-insecure
spec:
  port:
    targetPort: tcp-internal
  to:
    kind: Service
    name: facility-manager-service
    weight: 100
  wildcardPolicy: None